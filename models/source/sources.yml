version: 2

sources:
  - name: src
    database: sourcedb
    schema: mk_mall
    tables: 
      - name: nations
      - name: customers
      - name: regions
      - name: orders
        config:
          loaded_at_field: o_orderdate::timestamp
          freshness: 
            warn_after:
              count: 1
              period: day
          
